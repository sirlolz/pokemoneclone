<% if @npc.errors.any? %>
       <%= pluralize(@npc.errors.count, "error") %>
       prohibited this post from being saved:
    <% @npc.errors.full_messages.each do |msg| %>
       <%= msg %><BR>
    <% end %>
<% end %>

<%= form_for(@npc) do |f|%>
<%= f.text_field :name%>
<%=collection_select(:npc, :pokemon_ids, Pokemon.all, :id, :name)%>
<%= f.submit "create npc"%>
<%end%>
<%= link_to 'nevermind', npcs_path%>