<h2>All Users</h2>
  <% @users.each do |user| %>
      <%= link_to user.name, user_path(user) %><br>
      <%= link_to "Delete", user, method: :delete, data: { confirm: "YOU SURE?!?" } %><br>
  <% end %>